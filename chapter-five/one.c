#include <stdio.h>

/*
 * Exercise 5-1 of The C Programming Language
 *
 * As written, getint treats a + or - not followed by a digit as a valid
 * representation of zero. Fix it to push such a character back on the
 * input.
 */

int main(int argc, char **argv)
{
	return 0;
}
